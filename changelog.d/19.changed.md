The fork-only `--db <path>` / `LAST30DAYS_DB_PATH` store override is now an alias over the upstream `store.scoped_db()` mechanism: an explicit DB wins over `--save-dir` scoping, the prior-run library context reads the same DB the run writes, and `store.py` keeps only the env fallback that `briefing.py` and `watchlist.py` need.
