New opt-in `github_trending` source (`--search github,github_trending` or `INCLUDE_SOURCES=github_trending`): repositories rising this week from the public github.com/trending page, filtered to the topic, plus repositories created inside the research window in the topic from the keyless `/search/repositories` API. The two halves stay labeled and are never blended into one "trending" number.
