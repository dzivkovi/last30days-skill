Bridged corpus items now corroborate the same public article in ranking: a corpus file whose frontmatter `url` matches a web result transfers its vote (capped at one top-rank stream vote) to that result and tags it `in your files`, while the private file stays in the From your files block and the tag is stripped from every export and publish surface.
